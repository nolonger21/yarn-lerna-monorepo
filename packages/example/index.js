const { test } = require('@etherfe/pkg-a');
const merge = require('lodash/merge');

console.info('example', test);
test();
console.info('lodash222-111', merge);
console.info('lodash222-3333-1', merge);
console.info('lodash222-444-111', merge);